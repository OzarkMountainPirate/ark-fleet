# secrets — never commit
.vault_pass
group_vars/vault.yml
*.retry

# built install media
bootstrap/*.iso

# live environment config (copy from the *.example.yml files)
inventory/hosts.yml
group_vars/all.yml
