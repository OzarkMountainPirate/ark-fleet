# host_vars/

Per-host overrides go here (e.g. a host that runs extra mods, or a second
instance). The map assignments themselves live in `inventory/hosts.yml` under
each host's `ark_instances`. Leave this empty if you don't need overrides.
