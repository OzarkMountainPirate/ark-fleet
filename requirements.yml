---
# ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general      # ufw, make, timezone, debconf
  - name: ansible.posix          # mount, firewalld/selinux helpers
  - name: arensb.truenas         # TrueNAS middleware (datasets, NFS exports, users)
